require('dotenv').config();  // ✅ Load environment variables
const express = require("express");
const mongoose = require("mongoose");  // No need for { default: mongoose } here
const app = express();

app.use(express.json());

MONGO_URL=mongodb+srv://username:password@cluster0.mongodb.net/Mongo_assign?retryWrites=true&w=majority